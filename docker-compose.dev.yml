version: '3'
services:
  api:
    env_file:
    - ./api/.env
    build:
      context: ./api
      dockerfile: Dockerfile.dev
    ports:
     - "8000:8000"
    volumes:
     - ./api:/api
    # depends_on:
    #   - database
  # web:
  #   build:
  #   ports:
  #   - "8001:8001"
  # database:
  #   image: "mongo:latest"
