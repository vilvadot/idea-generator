version: '3'
services:
  api:
    env_file:
    - ./api/.env
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
     - "8000:8000"
  #   depends_on:
  #     - database
  #   links:
  #     - database
  # database:
  #   image: "mongo:latest"